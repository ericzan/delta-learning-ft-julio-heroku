import { LevelStepPipe } from './level-step.pipe';

describe('LevelStepPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelStepPipe();
    expect(pipe).toBeTruthy();
  });
});
